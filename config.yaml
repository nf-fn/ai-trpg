ollama:
  url: "http://localhost:11434"
  model: "gemma2"
  timeout: 60s

voicevox:
  url: "http://localhost:50021"
  speaker: 1
  timeout: 30s

whisper:
  model: "base"

server:
  port: 8080

paths:
  web: "web"
  rules: "rules"
  scenarios: "scenarios"
